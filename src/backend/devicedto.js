module.exports =  class DeviceDTO {
        constructor(id, name, description, state, type) {
                this.id = id;
                this.name = name;
                this.description = description;
                this.state = state; 
                this.type = type;
        }
       


}